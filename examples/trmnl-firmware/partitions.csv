# TRMNL firmware on epdInky ESP32-P4/C6 rev.2 - partition table
#
# Flash is 16 MB (W25Q128JVS).
#
# Two OTA slots because TRMNL's /api/display response can carry
# `update_firmware` + `firmware_url`, and esp_https_ota needs somewhere to put
# the new image. 3 MB each is generous for a pure-IDF build (no Arduino core),
# which leaves nearly 10 MB for the image cache.
#
# `storage` holds the LittleFS image cache: the last-displayed frame, so a
# differential refresh survives deep sleep (PSRAM does not).
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
otadata,    data, ota,     0xf000,   0x2000,
phy_init,   data, phy,     0x11000,  0x1000,
ota_0,      app,  ota_0,   0x20000,  0x300000,
ota_1,      app,  ota_1,   0x320000, 0x300000,
storage,    data, spiffs,  0x620000, 0x9E0000,
