# epdInky ESP32-P4/C6 rev.2 partition table
#
# Flash is 16 MB (W25Q128JVS). The default single-app table only gave the
# application 1 MB, which Wi-Fi plus BLE overruns.
#
# nvs and phy_init keep their default offsets and sizes so data already stored
# on the device (including Wi-Fi calibration) survives this change.
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  4M,
